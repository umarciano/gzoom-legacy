/*

Maps s.p.a. 
This script need to be loaded after jquery.js in order to resolve conflict with prototype

After this use jquery like below example: 
$j(document).ready(function(){
       $j("div").hide();
});


*/

var $j = jQuery.noConflict();
