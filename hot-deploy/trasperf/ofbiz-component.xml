<?xml version="1.0" encoding="UTF-8"?>
<ofbiz-component name="trasperf"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="http://ofbiz.apache.org/dtds/ofbiz-component.xsd">
    <!-- define resource loaders; most common is to use the component resource loader -->
    <resource-loader name="main" type="component"/>
    <resource-loader name="report-definition" type="component" prefix="config"/>

    <!-- place the config directory on the classpath to access configuration files -->
    <classpath type="dir" location="config"/>
    <classpath type="dir" location="script"/>

    <!-- load single or multiple external libraries -->
    <classpath type="jar" location="build/lib/*"/>
    <classpath type="jar" location="lib/*"/>

    <!-- entity resources: model(s), eca(s), group, and data definitions -->
    <!-- <entity-resource type="model" reader-name="main" loader="main" location="entitydef/entitymodel.xml"/> -->
    <!-- <entity-resource type="eca" reader-name="main" loader="main" location="entitydef/eecas.xml"/> -->
    <entity-resource type="data" reader-name="gplus" loader="main" location="data/TrasPerfTypeData.xml"/>
    <entity-resource type="data" reader-name="gplus" loader="main" location="data/TrasPerfSecurityData.xml"/>
    <entity-resource type="data" reader-name="gplus-demo" loader="main" location="data/TrasPerfDemoData.xml"/>

    <!-- service resources: model(s), eca(s) and group definitions -->
    <service-resource type="model" loader="main" location="servicedef/services.xml"/>
    <!--
    <service-resource type="eca" loader="main" location="servicedef/secas.xml"/>
    <service-resource type="group" loader="main" location="servicedef/groups.xml"/>
    -->

    <!-- <test-suite loader="main" location="testdef/TrasPerfTests.xml"/> -->

    <!-- web applications; will be mounted when using the embedded container -->
    <webapp name="trasperf"
        title="TrasPerf"
        server="default-server"
        location="webapp/trasperf"
        base-permission="OFBTOOLS,TRASPERFMGR"
        mount-point="/trasperf"/>
</ofbiz-component>