# ═══════════════════════════════════════════════════════════════════════════════#patch=dev

# GZOOM - Custom Properties - DEVELOPMENT Environment# AMBIENTE SVILUPPO LOCALE

# ═══════════════════════════════════════════════════════════════════════════════

# Questo file contiene le configurazioni specifiche per l'ambiente di SVILUPPO# Cache settings (brevi per sviluppo)

# Per attivarlo, decommentare '#patch=dev' in custom.propertiescache.default_expireTime=10000

# ═══════════════════════════════════════════════════════════════════════════════cache.other_expireTime=10000



# CACHE CONFIGURATION (Development - short expiration times)# POSTGRES - Locale

cache.default_expireTime=10000entityengine.datasource-name-prefix=local

cache.other_expireTime=10000entityengine.datasource-type=postgres

entityengine.jdbc-uri=jdbc:postgresql://localhost:5433/gzoom_dev

# DATABASE CONFIGURATION - PostgreSQL Developmententityengine.jdbc-password=postgres

entityengine.datasource-name-prefix=localentityengine.jdbc-schema=

entityengine.datasource-type=postgresentityengine.jdbc-username=postgres

entityengine.jdbc-uri=jdbc:postgresql://localhost:5432/gzoom_deventityengine.jdbc-driver=org.postgresql.Driver

entityengine.jdbc-password=postgres

entityengine.jdbc-schema=# Log level per sviluppo

entityengine.jdbc-username=postgreslog.level=DEBUG

entityengine.jdbc-driver=org.postgresql.Driver

# ═══════════════════════════════════════════════════════════════════════════════
# SSO SAML CONFIGURATION - DEVELOPMENT
# ═══════════════════════════════════════════════════════════════════════════════

# Enable/Disable SSO SAML Authentication
sso.saml.enabled=false

# SAML Service Provider (GZOOM) - Development URLs
sso.saml.sp.entityid=http://localhost:8080/gzoom-saml-web/metadata.jsp
sso.saml.sp.acs.url=http://localhost:8080/gzoom-saml-web/acs.jsp
sso.saml.sp.slo.url=http://localhost:8080/gzoom-saml-web/sls.jsp

# Frontend URL - Development
sso.frontend.url=http://localhost:4200

# Identity Provider Configuration (TO BE CONFIGURED)
sso.saml.idp.entityid=
sso.saml.idp.sso.url=
sso.saml.idp.slo.url=
sso.saml.idp.certificate=

# Debug mode - Always enabled in development
sso.debug.enabled=true
