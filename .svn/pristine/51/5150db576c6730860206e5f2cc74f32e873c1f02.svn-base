//Tolgo dai breadcrumbs i leading '>'
$j("div#header-breadcrumbs-th").each( function() {
    var str = $j(this).html();
    str = str.replace("&gt;","");
    $j(this).html(str);
});
$$('div.qtip').each(function(element) {
    element.remove();
});